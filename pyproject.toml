[project]
name = "thermo-charts-workspace"
version = "0.1.0"
description = "ThermoFisher mzML analytics dashboard - monorepo workspace"
readme = "README.md"
requires-python = ">=3.11"

# This is a workspace root - actual code is in apps/
